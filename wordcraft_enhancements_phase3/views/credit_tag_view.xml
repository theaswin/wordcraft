<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Tree View -->
    <record id="view_cred_tag_tree" model="ir.ui.view">
        <field name="name">cred.tag.tree</field>
        <field name="model">cred.tag</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="color" widget="color_picker"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_cred_tag_form" model="ir.ui.view">
        <field name="name">cred.tag.form</field>
        <field name="model">cred.tag</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="color" widget="color_picker"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_cred_tag" model="ir.actions.act_window">
        <field name="name">Credit Tags</field>
        <field name="res_model">cred.tag</field>
        <field name="view_mode">list,form</field>
    </record>

</odoo>
